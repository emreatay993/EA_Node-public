"""Runnable Qt/QML UX mockups for COREX node-editor surfaces.

These are real PyQt6 + Qt QML harnesses (not static HTML) so a mockup renders
with the same engine, fonts, theme tokens, and components as the running app.
"""
