# Fresh-session implementation prompt

Use large-plan execution to resume and complete the COREX Mechanical Catalogue plan in the repository root. Work on branch `codex/mechanical-catalogue`, whose planning baseline was `7f5304b4`. Verify the actual repository root, branch, HEAD, Git status and active workers before writing. Preserve every unrelated dirty or untracked file. The current on-disk documents are the authority even if this prompt differs from older conversation summaries.

Read the full repository `AGENTS.md`, then read these files in full before assigning work or editing: `docs/PLANS/MECHANICAL_CATALOGUE/PLAN.md`, `docs/PLANS/MECHANICAL_CATALOGUE/TASK_LEDGER.md`, `docs/PLANS/MECHANICAL_CATALOGUE/BACKEND_DECISIONS.md`, `docs/PLANS/MECHANICAL_CATALOGUE/REFERENCES.md`, `docs/PLANS/MECHANICAL_CATALOGUE/VISUAL_BASELINE.md`, and `docs/specs/perf/MECHANICAL_CATALOGUE_CAPABILITY_PROOF.md`. After any coordinator compaction, restart or handoff, reread the full plan and ledger, inspect Git and active workers, verify accepted commits, and reread the current backend/visual references relevant to the next task. Do not reconstruct state from a short summary.

Implementation, disposable fixture creation, necessary licensed Ansys 2026 R1 verification, and 18 separate local task commits are authorized. Do not push. Do not modify user production models or kill unrelated processes. Use the project venv. Run one Ansys-heavy cohort at a time, retain exact owned process identities, and reuse existing retained fixtures and evidence. Do not repeat passed cohorts or the retained solve; repeat a targeted check or heavy probe only when a change, failure, or unresolved concern requires it, and otherwise run only uncovered required fixture families. The instruction to stop belonged to the planning session and does not pause this fresh implementation session.

The coordinator owns requirements, decisions, acceptance and the single task ledger; only it edits `TASK_LEDGER.md`. Keep coordinator context compact; delegate detailed source/API investigation, implementation, testing and evidence review. Use fresh bounded Sol implementation workers, one writer at a time. Start T01 with a fresh Sol worker, reusing partial files and retained evidence without resuming an old agent ID. Use fresh Sol reviewers for substantial chunks and Astra for the final T18 integration review. Follow all 18 task subjects, dependencies, scopes, checks, reviews, commit subjects and hash convention. Each accepted task gets one commit with its code, tests, relevant docs/maps and ledger update; never combine, squash or create metadata-only commits. After coordinator compaction, restart or handoff, reread the complete plan and ledger and reconcile Git/workers. Continue through T18 without extra resume permission unless a real current-scope block is proved.

Start from TASK_LEDGER.md's current T01 status. The full 261 qualification evidence is retained, including both native archive families, model-only exports, background queries, tables, cameras, images, scripts and snippets. The user approved two narrow result-state amendments: ForceReaction accepts only probes already `By=Time`, never assigns read-only By, restores DisplayTime and rejects other modes; a configured result initially `By=Time`/`SetNumber=0` may retain and report a valid positive inactive SetNumber only when the public setter rejects zero, while restoring By/DisplayTime/CalculateTimeHistory exactly. All other restoration failures remain fatal. Preserve historical failed JSON and use the separate approved-policy report. Never reopen the retired 2025 R2 environment block. Release 261 is the sole initial reference, minimum and acceptance release.

Archiving is source-family native and this distinction is mandatory. A standalone Mechanical source supports `.mechdb`, `.mechdat`, and native `.mechpz` through Project.Archive/Unarchive. A Workbench source supports native whole-project `.wbpj` and `.wbpz` through Workbench Save/Archive/Unarchive, plus separate model-only selected exports to `.mechdb` and `.mechdat`. For the selected-model export only, use the already-proved native Workbench Model.Export `.dsdb` bridge, open it in a separate same-release standalone owner, then SaveAs. Do not use `.dsdb` to create an archive or preserve results. Reject `.mechpz` for a Workbench source and guide the user to `.wbpz`; never silently fall back, alias source families, rename files, copy internal databases, reattach omitted results, or continue investigating a result-preserving `.dsdb` route. T15 stays the model-only selected-export task. It has no archive output, result-transfer acceptance, or complete-archive claim.

Keep the three Save inclusion controls as typed Boolean ports, default `True`: `include_results`, `include_user_files`, and `include_external_imported_files`. Every input control has a typed port even when collapsed or inactive. Retain inactive ports, disable them and do not consume them. For Workbench `.wbpz`, map the controls to `IncludeSkippedFiles`, `IncludeUserFiles`, and `IncludeExternalImportedFiles`. For standalone `.mechpz`, consume only the two supported ArchiveSettings choices; external imported files stays inactive. Non-archive and model-only export formats consume none. Keep `FailIfMissingFiles=True`; a missing requested file fails. Preservation claims are conditional on enabled controls. Reports must state chosen omissions accurately and must not call an intentionally partial archive complete. Preserve source hashes, staged-publication rollback and destination recovery.

Preserve the other locked contracts. Every run reloads a fresh source and ordered mutations use explicit Save in the same dependency chain. Background always closes at terminal cleanup. A successful Interactive window remains inspection-only until the next graph-run start in the same workspace or workspace/application shutdown. Before any next run in that workspace, retire all of its retained Interactive instances even if the run omits Open; a run in another workspace leaves them alone. Failed/cancelled runs close all owned processes with bounded cleanup, and expired handles are never admitted. Carry workspace identity in lifecycle metadata; do not create an active global pool or key ownership only by Open.

Search uses the fixed COREX background definitions, exact opaque source IDs and unknown-data failure semantics; never call or retry native Filter, Find or IsObjInTreeView and never inspect table cells. Case-insensitive text uses Unicode `casefold()`. Exact compares the whole query and display text without Name tokenization. Contains uses one whole substring except Name + Contains, which splits unquoted text on Unicode whitespace and ANDs the terms in any order. Preserve interior whitespace for Exact, ordinary Contains and bare Property Value operands; only Name + Contains tokenizes whitespace. Empty or whitespace-only unquoted text keeps unrestricted semantics; quotes in Name are literal and other nonempty plain text remains unchanged. Property Value may use one optional `Property Name = Property Value` pair, split at the first `=` outside complete JSON-style double-quoted operands. Trim only Property Value operands' syntactic edge whitespace; preserve decoded quoted content, bare interior whitespace/backslashes/apostrophes and later equals signs. With an outside `=`, reject an empty caption; an explicitly empty value matches only an available empty display in both Match modes and never means unrestricted search. Without an outside `=`, no caption restriction applies. Reject malformed quotes/escapes. Examples: `Comment = "Load = 100 N"` and `Comment = "Bolt \"A\""`. Resolve typed picker identities before this grammar and keep them opaque/exact. Add no regex, numeric comparison or wider query language. T06 must pin these cases.

FEA Table performs actual supported native model-definition, result/probe and worksheet extraction from existing data and may evaluate selected existing results without implicit Solve. Use no Mechanical-specific QML branches. T16 must verify the real production QML at the specified fonts/themes/scales, every typed port including inactive/collapsed controls, group-collapse wire identity and the approved visual baseline.

For each task, run its smallest proving check, obtain the required independent review, update affected docs/maps in the same change, and create the exact dedicated local commit only after acceptance. Stage only task-owned files and inspect staged paths/content/subject for private provenance. At T18 run the plan's focused integration, appropriate repository verification and documentation checks, publish the neutral QA matrix through the specs index, and obtain the independent Astra review. Finish with accepted task hashes, checks, evidence paths, remaining material limitations, confirmation that unrelated work was preserved, and confirmation that nothing was pushed.
