# Mechanical Catalogue — Task Ledger

Overall status: **COMPLETED — T01–T18 ACCEPTED**.

On 2026-09-07 the user explicitly authorized implementation, required verification with disposable fixtures and available licensed Ansys installations, and separate local task commits. The earlier planning-only authorization is historical. T01 is accepted under the user-approved 261 result policies; production implementation starts at T02. Layout approval alone is not compatibility evidence.

The user subsequently directed: **use 2026 R1 as the reference instead of 2025 R2**. The plan now requires 261 as the sole initial reference, minimum supported release and acceptance release. The former missing-252 block is superseded by this explicit scope change; 252 and earlier releases are outside initial support. All other task and acceptance requirements remain in force.

Subsequent execution direction: new implementation workers and intermediate independent reviewers use **Sol**; the final T18 integration review uses **Astra**. The coordinator delegates detailed implementation and evidence review. The user's archive-inclusion amendment is recorded in the plan: `include_results`, `include_user_files`, and `include_external_imported_files` are typed Boolean input ports, default True, with format-specific applicability. Requested inclusion must be honored by the applicable native archive route.

Latest user decision: **use native archiving, not `.dsdb`, update the plan and stop this session**. Workbench archives use native whole-project `.wbpz`; standalone Mechanical archives use native `.mechpz`. The previous required Workbench-to-standalone archive/result-transfer route is retired. Separate selected-model `.mechdb`/`.mechdat` exports remain model-only; their `.dsdb` bridge is never an archive or result-preservation route. This resolves the former scope question without accepting T01. Resume only in the fresh implementation session using the [full handoff prompt](IMPLEMENTATION_PROMPT.md).

The user subsequently approved the recommended wording clarifications. Archive guarantees follow the native route and enabled inclusion choices; model-only exports have no result/dependency-transfer gate. Successful Interactive windows remain inspection-only until any next graph run starts in the same workspace, that workspace closes, or the app shuts down; other workspaces do not close them. Background, failed and cancelled runs close their owned processes, and expired handles remain unusable. Search now has an explicit Exact/Contains/quoted-operand grammar. The next session starts a fresh Sol T01 owner and reuses saved work; historical package/probe statements are not current prerequisites.

Authority: read the **entire** [PLAN.md](PLAN.md), this entire ledger, and current repository AGENTS.md before work and after every compaction, restart, or coordinator handoff. The coordinator alone updates task status/ownership/acceptance. Do not create another ledger or infer completion from a worker's summary.

## Current checkpoint

- T01 is independently accepted under both explicit user amendments. ForceReaction requires an already-Time probe, restores DisplayTime without assigning By, and rejects other modes. Only the initial By=Time/SetNumber=0 case with native zero rejection may retain/report a valid changed SetNumber after exact By/DisplayTime/CalculateTimeHistory restoration; every other restoration failure remains fatal. Tree/camera/graphics restoration stays independently exact.
- Accepted evidence: ignored `artifacts/verification_logs/mechanical_catalogue/T01/qualification_approved.json` (schema 3) validates against the unchanged raw native observations. Historical `qualification.json` and `qualification_v2.json` retain their failed former-policy outcomes. Source-family native archives, model-only exports, all eleven background queries, real imported IDs, cameras, definition/result/probe/worksheet tables, images, script scope and load-step snippets are qualified within T01's API boundary.
- Verification: `venv/Scripts/python.exe -m pytest tests/mechanical_catalogue/test_probe_contract.py -q` passes 17 tests; actual approved-report validation with `--evidence-root artifacts/verification_logs/mechanical_catalogue/T01` passes. Compilation, traceability, canonical Markdown links and explicit plan-package link checks pass. Independent Sol reviewer `t01_acceptance_review` approved the amended contract, actual evidence and T08 execution brief with no remaining findings. Implementation owner was `t01_qualification` (Sol).
- Reuse two retained solves: the original Workbench static fixture and one genuinely standalone modal solve authorized for the previously uncovered native standalone archive/modal family. No additional launch or solve was needed for policy acceptance. Exact cleanup reconciles 18 role/PID/creation identities with zero survivors. All 37 original top-level historical JSON report hashes were preserved. The task-created gRPC launch artifact was moved natively into ignored evidence after cleanup and is absent from the root.
- Repository: `<repo-root>`, branch `codex/mechanical-catalogue`, starting baseline `7f5304b47ade7d74ecfd2e4b32cb8f2b4ec1f253`. Each task gets its exact separate local commit; do not push. The T01 committed row uses its unique subject until the actual SHA can be carried by the next substantive task commit.
- Preserved unrelated starting SHA-256 values: `scripts/Strain_Gage_Positioning/modular_version/run.py` = `810d32787545a44259e8104be00ec8f447c8899c452a95faaa696fd6761432ba`; `tests/fixtures/graph_canvas_surface_snapshot.json` = `a8bc0b674fb19f0a06437c19ad090ca3890fba8ca1b30b2ae9a186ad6608f92a`; `docs/PLAN_COREX_Physical_Simulation_Backend.md` = `f1709cd27cdd97141e354ab0644b3602f8ea43ebefbb294b0c5fa4578c6788f2`; strain candidate CSV beside the script = `468c04c09df327871ed6cd947ef58e8f26412d632a1e969c3c56303dfc44061b`. Preserve the unrelated Physical Simulation index entry and leave it unstaged.
- Current milestone: all T01–T18 tasks are independently accepted. T17 is committed at `ec8a8091b569165846f7ea8dc5f7ee237bb14dad`; the final T18 committed row uses its exact unique subject, and the final report supplies the resulting SHA. No work or user decision remains pending. Both original solves and accepted evidence were reused; nothing was pushed.
- Final delegation: Sol `t18_integration_qa` and independent Astra `t18_integration_review` completed. Final Astra approval is retained in ignored `T18/independent_review.json`; the original CLI event log remains under `T11/worker_server.jsonl`. Earlier task contexts are complete. The coordinator retires only the task-owned CLI helper at closeout; no saved app tasks or extra ledgers were created.
- During T11 the user authorized GPT-6 Astra at xhigh for the specific worker-startup/workspace-retirement fix. Sol independently approved the bounded `retirement_astra` change, then completed the remaining T11 implementation and acceptance. Retirement waits occur outside the lifecycle lock, with revision/generation/reservation checks after the wait; a never-dispatched physical generation alone can return zero without a transport request. Warm owners retain the unchanged acknowledgement deadline. Other task routing and the fresh final T18 Astra review are unchanged.
- T05 accepted evidence under ignored `artifacts/verification_logs/mechanical_catalogue/T05/`: `workbench_writer_staged_save.json` proves the native staging Save/349,165-byte archive; `workbench_direct_selected.json` and `workbench_interactive_selected.json` prove isolated working projects, stable shared systems, 611 descriptor rows and the visible Mechanical editor. Native RunWB2 is created suspended and assigned to an owned job before resume, avoiding WMI ownership escape. `registered_open_v5.json` plus `registered_open_raw_events_v4.json` prove the actual registered graph node, 868 Info rows, matching Model/Info identity, unchanged source and zero owned survivors. Original standalone format/mode reports remain reusable. Historical failed reports are unchanged; no solve was added.
- T05 verification: 212 tests PASS with `venv/Scripts/python.exe -m pytest tests/mechanical_catalogue tests/test_addon_catalog.py tests/test_addon_registry_contributions.py tests/test_corex_contract_catalog.py tests/test_repo_owned_node_documentation.py tests/test_main_bootstrap.py tests/test_packaging_configuration.py tests/test_builtin_integration_function_migration.py tests/test_mars_function_migration.py tests/test_remaining_builtin_function_migration.py tests/test_tabular_function_migration.py -q -n 0`; independent reviewer `t05_backend_review` passed 180 focused tests and approved the final diff/native evidence. Ruff, maps, traceability, Markdown links and diff whitespace pass. Full terminal output remains in worker history; live logs/raw events are in the task artifact directory. Source/frozen command and bootstrap/package configuration checks pass; no packaged executable E2E build is claimed or automatically required at T18. Later releases require the implemented capability handshake; live acceptance remains 261 only.
- T05 implementation follow-through: decorated functions must pass their supplied `settings`; connected values override them. Source-document selector identity binds the normalized path and source hash, while run/session/revision remain transient. The bounded selector index retains an immutable TableValue reference for identity comparison, preventing Python ID reuse. Canonical module launch is required for source/dev verification. Info accurately reports unavailable native getters as partial metadata.
- T06 accepted evidence: ignored `T06/registered_open_search.json` and `registered_open_search_raw_events.json` under the same Mechanical artifact root prove actual registered Open→Search through Workbench Background, 32 Objects/485 Properties/32 Details, matching Model identities, unchanged source tree and zero survivors among two observed owned PID/creation identities. The live query is unrestricted Name; deterministic production-path tests and retained T01 evidence cover the complete grammar and difficult availability rules. The final metadata-only refinements needed no repeated native cohort or solve.
- T06 verification: 149 tests PASS in `T06/focused_tests.log`; independent `t06_semantic_review` passed 118 focused tests and approved the final semantic/transport/native evidence. The acceptance command is `venv/Scripts/python.exe -m pytest tests/mechanical_catalogue/test_search_tree.py tests/mechanical_catalogue/test_property_edit.py tests/mechanical_catalogue/test_open_model.py tests/mechanical_catalogue/test_owner_protocol.py tests/mechanical_catalogue/test_catalogue.py tests/mechanical_catalogue/test_contracts.py tests/mechanical_catalogue/test_session_lifecycle.py::test_registered_run_cancellation_closes_the_owned_session tests/test_corex_contract_catalog.py tests/test_repo_owned_node_documentation.py tests/test_builtin_integration_function_migration.py tests/test_mars_function_migration.py tests/test_remaining_builtin_function_migration.py tests/test_tabular_function_migration.py -q -n 0`. Ruff, maps, traceability, links and diff whitespace pass; source-test and route indexes were regenerated by their repository scripts. The exact generated remote script is exercised with representative objects and cell-access traps. The search spool reuses the generic value codec, a minimal frozen catalogue and exact carrier/identity validation.
- T07 accepted evidence: `T07/registered_definition.json`, unchanged raw events, and `native_validation_v5.log` prove registered Open→Search→FEA Table→Signal Plot on the retained standalone definition fixture. Exact constant/tabular/formula values, native independent Time, source `sec` versus SI `s`, Definitions and API-sample notes are retained. A separate owned no-model-mutation check proves `0 C → 273.15 K` with the original quantity unchanged. The graph produced a 44,757-byte plot, preserved source/source-tree hashes and left no scoped processes. No solve was added. The identical server script and small-receipt native file boundary have focused fake-server proof; no live T07 Workbench-family claim is made.
- T07 verification: 172 focused tests PASS in `T07/focused_tests.log`, using the exact T06 acceptance command above with `tests/mechanical_catalogue/test_definition_tables.py` added first; `venv/Scripts/python.exe -m pytest tests/test_scientific_worker_transport.py tests/test_signal_plot_scientific_integration.py -q -n 0` passes 23 tests. Independent `t07_data_units_review` passed 105 scientific/definition/plot tests and accepted the final diff/evidence. Ruff, maps/indexes, source-test index, traceability, links and diff whitespace pass. Full-fidelity shape checks reject unequal populated columns, preserve duplicate labels positionally and project null only for Free/no-sample states. Native Fields use exact object attributes, with no InternalValue fallback. Native and owner file transfers retain canonical 256/512 MiB limits and account for encoded expansion; acceptance self-tests stay out of production. Result/probe/worksheet families remain explicit unsupported cases until T08.
- T08 accepted proof is `T08/accepted_receipt.json`, hash-linking unchanged historical source report `registered_results.json` (`ad824f32…`) and separate SI report `registered_si.json` (`538fde85…`). All three read-only validators pass: canonical module `T08.probe_registered_results --validate-existing`, `T08.probe_registered_si --validate-existing`, and `T08.probe_registered_si --validate-receipt` under `artifacts.verification_logs.mechanical_catalogue`. Exact configured/ForceReaction/spatial/modal values, sets, native relations/units, plots, one approved drift warning/Definitions note, source integrity and eight PID/creation identities with zero survivors are proved. No solve was added. Historical failures and the old source-only SI limitation remain separate from current acceptance.
- T08 verification: `venv/Scripts/python.exe -m pytest tests/mechanical_catalogue/test_result_tables.py tests/mechanical_catalogue/test_worksheet_tables.py tests/mechanical_catalogue/test_definition_tables.py -q -n 0` passes 56 tests (`T08/table_tests_final.log`); the wider T07 command above, with the result/worksheet modules added first, passes 204 (`T08/focused_tests_final.log`). Independent `t08_numerical_review` accepted current code, native evidence and validators; Ruff/maps/traceability/links/catalog/diff checks pass. Worksheets reuse T01 live API evidence plus current generated-script production tests for rows, mixed types, units, unsupported sources and no solve/remesh; T08 registered native cohorts cover results/probes/modal/SI rather than a new live worksheet run.
- T08 implementation follow-through: PlotData units come from each native ResultVariable and can differ from display units; fetch a fresh snapshot after each evaluation. Native QuantityName is absent for the tested result columns and remains absent in Definitions. SI uses native unit-driven target lookup and copy conversion when names are absent, with exact precision and an affine-ambiguity guard; no hand unit map or invented dimension. Spatial support is the qualified Body/Node/Values shape, and unsupported probes/worksheets fail explicitly. Result/tree/graphics verification failures retire the session; inactive Rows/sets is not consumed for definitions/worksheets. Current source modal native relations are preserved, even when they differ from the earlier archived fixture's relation classification.
- T01 changes qualification helpers/docs only; application ownership is unchanged and needs no agent-map update. T02 and later source owners must update affected maps/COVERAGE as required.
- T09 accepted proof is `T09/accepted_receipt.json`; canonical module `artifacts.verification_logs.mechanical_catalogue.T09.probe_registered_camera_views --validate-existing` verifies linked raw hashes and the retained T01 camera restoration proof. Actual registered Open→Camera Views returns saved index 0 followed by Current view, finite public fields in mm, exact typed/Details identity, unchanged source SHA `c24fdeb8…`, no final operation XML and no survivors for four recorded PID/creation identities across the second/final attempts. The first failed attempt did not persist exact process identities; its raw report and diagnostic work directory remain, and a weaker work-token scan found no related live process. No solve was added; historical failures remain failed.
- T09 verification: 17 focused camera tests PASS (`T09/focused_tests.log`) and 97 broader tests PASS (`T09/broader_tests_final.log`); independent `t09_schema_review` passed 20 camera/shared Panel checks and 44 catalogue/contract checks, then approved final count-only corrections and the receipt validator. Ruff/maps/traceability/links/diff checks pass. Current generated-script tests cover success, mid-enumeration error/cooperative cancellation, nullable-unit consistency, malformed XML, exact restoration/temp-view deletion and retirement on restoration failure. The registered native operation also enforces its own exact before/after restoration gate. Forced process termination relies on accepted T03 cleanup/source isolation, with no presentation-restoration claim after process death.
- T09 reproducible commands: `venv/Scripts/python.exe -m pytest tests/mechanical_catalogue/test_camera_views.py -q -n 0` (17); `venv/Scripts/python.exe -m pytest tests/mechanical_catalogue/test_catalogue.py tests/mechanical_catalogue/test_contracts.py tests/mechanical_catalogue/test_owner_protocol.py tests/mechanical_catalogue/test_property_edit.py tests/test_corex_contract_catalog.py tests/test_repo_owned_node_documentation.py tests/test_data_type_ui_projection.py tests/test_builtin_integration_function_migration.py tests/test_mars_function_migration.py tests/test_remaining_builtin_function_migration.py tests/test_tabular_function_migration.py -q -n 0` (97). The final catalogue contains 143 rows; its committed-blob/type-ID hashes are updated in the existing classification inventory.
- T10 accepted proof is `T10/accepted_receipt.json` (SHA-256 `67565358a5b127f9bc01377832cb75b56740dbc844e74e7e94e7a1f7581a523e`), linking unchanged raw attempts, fixture preparation, the registered six-image batch and targeted Fit=True graph. The two object branches each contain three 640×400 images; ImageValue and published-file hashes agree. A separate one-image preview reaches Media Panel, and the Path consumer completes. All six named-view close-ups preserve Fit=False; the inspected Fit=True image shows complete centered geometry. Source/fixture hashes remain unchanged, all seven recorded PID/creation identities are absent, temporary outputs are gone, and no solve was added. The first attempt retains its Media Panel list-to-item wiring failure without changing the successful batch-capture observation.
- T10 verification: `venv/Scripts/python.exe -m pytest tests/mechanical_catalogue/test_image_export.py -q -n 0` passes 26 (`T10/focused_tests.log`); related Mechanical checks pass 97, runtime/image/Media checks 13, and catalogue/migration checks 26. Complete exact commands and log hashes are in the receipt's `checks` entries. Independent `t10_graphics_review` accepted the final 26-test invocation-isolation delta, earlier 37 image/catalogue/property checks, seven inspected PNGs and a read-only receipt/identity/hash validation. Ruff/maps/traceability/links/diff checks pass. `T10.build_accepted_receipt` is the evidence builder with assertions and rewrites the current receipt; do not treat it as a read-only validator. Publication preflight, no-clobber races, identity-aware rollback/recovery, inactive file controls, typed selectors, exact branch/order/dimension validation and explicit approved result-drift warnings are covered. Native captures use body objects; current generated-script result checks reuse T08's native result API/restoration qualification.
- T11 accepted proof is ignored `T11/acceptance_receipt.md` (SHA-256 `4a74b9cdebf82896417937943f9b7b22d92526831b9ea66e8b1038a052620b03`), linking 21 hashed artifacts and complete retained commands/logs. Canonical modules `T11.native_probe` and `T11.graph_probe` under `artifacts.verification_logs.mechanical_catalogue` prove native each-analysis/model-once scope, partial failure without a success Model, revisions/stale references/fresh source, and normal registered CorexRuntime→worker→Mechanical routing. Six graph settlements were accepted; Open/old Search expired, the Script/new Search/Table/Plot remained current, and the internal invalidation request stayed private. The table is Time 1.0 sec / Force 750 N with one plot image. Source SHA `81d8e1…626e` is unchanged, final UUID work root is absent, all 12 recorded PID/Windows-FILETIME-ns identities are absent, and no solve was added. Historical failed, bypass-derived and pre-wording evidence remains separate; bypass evidence is diagnostic only. The fresh physical generation used the normal zero/no-wire retirement path, not a claimed ACK.
- T11 verification: retained final focused command in the acceptance receipt passes 16 (`final_focused.log`), catalogue/migration/source-index command passes 36 (`final_catalogue_migrations.log`), and static/docs checks pass. Startup evidence under `T11/startup_fix_841d663abdb7471f9b51bfa35e09684e/` retains the deterministic failing reader-lock trace and corrected checks: first runtime ID 1 pass, three IDs serial 3, process/race/cancellation 33, lifecycle 15, backend reservations 11 plus 3 subtests. The user-approved Astra xhigh fix was independently accepted by Sol after six focused cases. Retained Sol reviewer `t11_mutation_review` then accepted the complete script/native/normal-graph proof and all linked hashes. Earlier 268-test Mechanical and 198-test/110-subtest results are supporting tool history, not substitutes for retained final logs. Same-run invalidation affects only current observations accepted by that run; pending captures/history/unrelated branches are preserved. Stdout is bounded during capture and successful receipts use the scientific spool. Each Model revision has a distinct immutable wrapper; AnalysisSettings is excluded from analysis classification.
- T12 accepted proof is ignored `T12/acceptance_receipt.md`, linking 23 hashed artifacts with full exact commands. Native registered CorexRuntime/process-worker runs cover two Static Structural analyses, All and selected `[1,3]`, explicit fixture SaveAs/reopen, six unchanged ObjectIds on owned update, and the expected unowned-name collision failure. Native WriteInputFile outputs contain All markers at steps 1–3 and selected markers only at 1/3, zero SOLVE lines when disabled and one per step when enabled; no solver launch occurred. Original source SHA `81d8e1…626e` is unchanged, all ten PID/Windows-FILETIME-ns identities and UUID work roots are absent. Attempts 1–3 remain unchanged; attempt 3's harness exit was an incorrect aggregate-run expectation, while its retained node events prove the intended collision failure. `T12.validate_existing` validates retained data and rewrites the current Markdown receipt; it is not a read-only validator. Independent review reproduced assertions without that rewrite.
- T12 verification: final focused command in the receipt passes 42 (`final_focused.log`), catalogue/migration/source-index command 71 (`final_catalogue.log`), and Ruff/maps/traceability/links/diff checks pass. Independent `t12_snippet_review` passed 42 focused cases, verified all 23 hashes/sizes, raw identities/input placement/cleanup, and accepted with no findings. Empty environment selection targets supported Mechanical/Static analyses; explicit unsupported phases fail before mutation. Exact markers, unowned collisions, inactive selected steps, full rollback through Report/spool/response construction, malformed/uncertain receipts and fatal restoration failure are covered. Native Unicode handling is shared with remote inspection text without altering Search matching semantics. Fixture saves prove owned updates only; they do not implement T13 saving or archives.

- T13 accepted proof is ignored `T13/acceptance_receipt.json` (SHA-256 `0b457c4eccf6f4d685b0c2563992cfa9e8cf89330b290badb78000e3bdea8437`), linking exact commands, log/raw/report hashes and retained recovery paths. Four unchanged reports supply 14 registered CorexRuntime/process-worker runs: each standalone format saves and independently reopens with the same six-command digest; modal archive inventories are eight result/solution files plus one user file, zero plus one with results excluded, and eight plus zero with user files excluded. Explicit source overwrite changes the disposable source and its next run observes those bytes; ordinary source bundle manifests remain unchanged. Fresh Model/Report identities agree, all 50 recorded PID/creation identities are absent, and no solve was added. Historical failed attempts remain unchanged; three reported recovery directories remain available.
- T13 verification: final focused command in the receipt passes 30 (`final_focused.log`), related script/snippet/owner/catalogue checks 106 (`final_related.log`), and catalogue/migration checks 31. Ruff/compile/maps/route-index/traceability/links/diff checks pass. Independent `t13_publication_review` passed 30 focused cases and the final no-write `T13.validate_existing` validator, checked all receipt/raw/log hashes and source/cleanup facts, and accepted without findings. The validator uses a minimal frozen catalogue; the separate builder rewrites the current receipt only. Late output serialization remains inside rollback protection, destination identities are captured before staging, existing files use atomic replacement with recovery, and native readers are disposed in finally. Standalone archives consume only the two applicable flags; nonarchives consume none. Native paths use compact separate staging/reopen directories. Workbench save/export remains T14/T15.

- T14 accepted proof is ignored `T14/acceptance_receipt.json` (SHA-256 `0db04c29c5f18242ed299a78880630db3447a19d9f576867d110105b9e764bc2`). Canonical module `artifacts.verification_logs.mechanical_catalogue.T14.validate_existing` is read-only and independently passes 273 referenced files, 14 successful registered graphs and 70 exact owner identities with zero survivors; three reported recovery roots remain. Native `.wbpj` and all-enabled `.wbpz` preserve the linked/shared project and independently reopen its selected models. Separate result/user/external exclusion evidence, requested missing-CDB Archive failure, new Model revision/connection generation and downstream use are accepted. Source primary/companion/external manifests remain exact; no solve was added.
- T14 verification: the receipt retains exact commands/log hashes for 98 affected tests, 8 final boundary checks, and final docs/hygiene checks. Independent Sol passed 9 focused HDF5/path/dependency cases, accepted the native gates, then ran the completed read-only validator once. The shared native JSON file route preserves Unicode without codepage fallbacks; Model-less systems remain in full project snapshots while Model selection skips them. Only a sole working-to-stage SHA difference on the qualified native `dp0/act.dat` may use the closed HDF5 logical comparator; every other file/phase, source check, archive flag and publication/recovery guard stays strict. HDF5 is lazy and directly declared for the add-on/ansys extra.
- T14 evidence limits remain explicit: the final no-external Save uses a hash-pinned 1.1-second timing insertion to exercise the mismatch branch; reviewed source is restored byte-exact and the separate reopen uses ordinary source. The proof establishes logical HDF5 equivalence with container-metadata drift, not timestamp-only byte changes. External exclusion means no embedding/rebase of the active registered reference; pre-existing unregistered STEP copies remain, so no absence-of-all-copies claim is made. Only the all-enabled archive cohort proves reopening with the original external location unavailable. Failed aggregates remain failed with successful subruns identified separately. The first successful `.wbpj` producer's exact source was not retained before replacement; its recorded hash/raw/output evidence remains without a fabricated replayability claim.

- T15 accepted proof is ignored `T15/acceptance_receipt.json` (SHA-256 `82a35d1be842dc98176a88137c72e15198b876c0917e4d6df6ca508547bf5be4`). Canonical read-only module `artifacts.verification_logs.mechanical_catalogue.T15.validate_existing` independently validates the consolidated evidence. Four native selected-model outputs (`SYS` and `SYS 2`, each `.mechdb`/`.mechdat`) independently reopen through registered graphs. Three post-fix exports prove returned Workbench source usability. The shared model has 31 objects/two analyses; the independent model has 22 objects/one analysis and its own marker. Geometry, loads, scopes, source manifests, three-system topology/shared group/design point and omission reporting are verified. Continuation cleanup records 119 native/owner identities absent at terminal cleanup and its one COREX worker absent after shutdown; the retained first publication separately records 30 cleaned identities. No new solve or archive was created.
- T15 verification: the project venv ran `-m pytest -n0 tests/mechanical_catalogue/test_workbench_model_export.py tests/mechanical_catalogue/test_owner_protocol.py::test_owner_protocol_survives_handshake_timeout_while_idle -q` (20 before the final formatter-only test addition); the exact added `test_workbench_model_export.py::test_model_export_report_omits_archive_only_workbench_proof` test then passed separately (1). Related command: `-m pytest -n0 tests/mechanical_catalogue/test_standalone_save.py tests/mechanical_catalogue/test_workbench_save.py tests/mechanical_catalogue/test_owner_protocol.py::test_real_owner_process_has_exact_identity_and_bounded_protocol tests/mechanical_catalogue/test_owner_protocol.py::test_owner_transport_crash_is_detected_without_touching_other_processes tests/mechanical_catalogue/test_owner_protocol.py::test_missed_explicit_shutdown_does_not_orphan_owner tests/mechanical_catalogue/test_owner_protocol.py::test_forced_parent_death_does_not_orphan_owner -q` (116). Catalogue command: `-m pytest -n0 tests/test_corex_contract_catalog.py::test_current_repo_owned_catalog_matches_current tests/test_repo_owned_node_documentation.py -q` (3). Hashed logs are in the receipt. Maps, traceability, links, Ruff, compilation, classification and diff checks pass. Independent Sol accepted the complete native evidence, focused mapping/idle/cancellation corrections and final no-write receipt validation with no remaining findings.
- T15 implementation and evidence limits: settings/Definitions use a validated transient native-ID map to the authoritative base tree; those IDs are removed before semantic comparison. Exact native Analysis Settings/WorkingDir attestation and all model-value/publication guards remain strict. An authenticated owner now waits for the next request without retaining the handshake's 10-second idle timeout; parent operation/cancellation/cleanup bounds are unchanged. Nonempty snippet/saved-view preservation reuses hash-verified T01 native outputs and current exact comparator tests; the current four-case fixture itself has no snippet/saved view. The first SYS mechdb publication predates the idle fix and its aggregate remains failed on downstream source inspection; its file is reused with a successful fresh reopen. A later oversized inspection-message failure remains failed; the corrected harness uses retained hashed UTF-8 files. One diagnostic lacked persisted cleanup identities; another reported a terminated-but-queryable launcher whose recorded wait had already returned zero. Neither is upgraded to native acceptance evidence. Final model-only proof omits two irrelevant archive-report fields; raw native reports retain their historical fields unchanged. Outputs omit results, user/imported files, Workbench topology/shared links, design points and dependencies.

- T16 accepted proof is ignored `T16/review_matrix.json` (SHA-256 `dd4c6def93e055fbbb52e95c2ed6cfe14b5b5291726548cf56a6a5918356e031`). Independent Sol inspected all 64 production node captures and four icon sheets, checked their hashes and the production-file/diagnostic records, and accepted all 52 typed inputs, 22 outputs, 16 sections, initial states, visible grips, shared textareas, connected/inactive controls and icon provenance. Windows/Direct3D11 captures record effective DPR1.0/1.5 using process-only factors0.5/0.75, actual graph/inline fonts10/16, title fonts12/18 and graph/canvas/host scales1.0. The fresh-default physical cohort separately verifies primary groups expanded, advanced groups collapsed, and ten unchanged edges through collapse/restore. No Windows display preference or Mechanical backend behavior changed; no Ansys process or solve was launched.
- T16 verification uses the project venv. `-m pytest -n 0 tests/mechanical_catalogue/test_controls.py tests/mechanical_catalogue/test_visuals.py tests/mechanical_catalogue/test_property_edit.py tests/test_node_title_icon_sources.py tests/test_node_title_icon_assets.py tests/test_port_flow_state.py tests/test_graph_scene_presentation_facts.py tests/test_plugin_declaration.py tests/test_decorator_sdk.py tests/test_registry_validation.py::RegistryValidationTests::test_settings_group_contract_and_registry_validation tests/test_registry_validation.py::RegistryValidationTests::test_node_documentation_metadata_defaults_and_normalization tests/test_serializer.py::SerializerTests::test_round_trip_preserves_and_normalizes_expanded_settings_group_ids -q` passes235 tests/121 subtests. `-m pytest -n 0 tests/graph_track_b/scene_model_graph_scene_suite.py -k settings_group -q` passes5. `-m pytest -n 0 tests/test_corex_contract_catalog.py tests/test_repo_owned_node_documentation.py tests/test_addon_catalog.py tests/test_addon_registry_contributions.py tests/test_graph_canvas_split_bridges.py tests/test_registry_agreement.py -q` passes63/90 subtests. The four-module graph-surface command in PLAN passed140 before the final review fixes; afterward it recorded139/140, with the Path Pointer display-fit case passing both writer and independent exact retries. Review traced this to transient relayout settlement outside changed T16 semantics; the failed aggregate remains failed. Independent source review passed31/17 subtests, catalogue3 and all icon/map/link/traceability checks. No full matrix replay was required for the final evidence-only light-sheet or comment-only banner corrections.
- T16 implementation and limits: generic declared fresh expansion defaults apply only at normal type-based creation; saved/copied/fragment states remain authoritative. Adapter constraints are distinct from freshly evaluated declarative and wire state. System disables only for the three known standalone suffixes. Central icon fallback is confined by trusted repository provenance; external packages retain verified generation-root admission. The light sheets now use actual production foreground `#17174b` on `#f6f8f8` (independent contrast15.628:1). Earlier DPR2, default/mixed-font, low-contrast and partially overwritten iterative captures are diagnostic only and excluded from acceptance. Ordinary shared path/dropdown elision remains at default card width; no Mechanical-specific QML or per-node geometry was added. Four final navigation-banner changes have preserved pre-edit copies and verified identical normalized ASTs in `T16/banner_ast_equality.json`; behavior and captures are unchanged.

- T17 accepted proof is ignored `T17/final_verification_receipt.json` (SHA-256 `9c600c151b843a26972b75e16a8cc568e489c4313ada453a6df72a64c8dae1c1`), linked to unchanged exact command/output log `T17/command_outputs.log` (`fdbe62fc1c6bcf41bc5895ced5c7556c7bb52bec38df913e2845520bcec0ba5b`). The three schema5 projects load through the normal serializer with exact surviving edges, registry-compatible ports/types/groups and blank authored paths. Windows serializer-output (CRLF) SHA-256: table `01dcde294c354226454a30d31872471bb5abda3022320cd8c7b4d7803352e512`; image `f39d0d1e60b0b8768aeb73344bd81c2244a05c4fd2d59edbbc840ea1404cb46e`; mutation `395aa3f2ec09772603bf75fc2a0923111b9be0b87267616d0f76dc091e7d6f13`. Git-canonical LF SHA-256: table `77aa0c731453247095e611d3cdd69f28e7c4b74c31071df916965eb0f23c20ac`; image `9b9fbad47c8d06765eeab283336960052d658f839378ed7fbe8e158b3443e672`; mutation `a18366a16b986b776fabf7c34dd8feb0d715769bd86c57ded5477260c9102a8f`. Their JSON and LF-normalized bytes are identical. The guide documents the exact catalogue, actual mutator `source_model` keys, limits, lifecycle, search grammar, tables, images, scripts/snippets and native save policies. Requirement/traceability/spec/map registrations and generated indexes are current.
- T17 verification: project venv `-m pytest tests/mechanical_catalogue/test_examples.py tests/mechanical_catalogue/test_catalogue.py tests/test_corex_contract_catalog.py tests/test_repo_owned_node_documentation.py -q -n 0` passes27; `-m pytest tests/test_plugin_declaration.py -q -n 0` passes122. The direct registered-source `test_catalogue.py::test_registered_function_forwards_unconnected_properties_as_settings` passes1 and the full catalogue module passes6. Independent Sol passed28 with the unknown-private-field check,122 decorator cases and9 index-hygiene cases, plus Ruff/traceability/links/maps/index freshness/eight-file package audit. Initial reviewer basetemp setup and child-Git ownership fallback errors were resolved in isolated per-command configuration, without global config or repository regeneration under the wrong inventory. The retained writer log preserves the interim stale-index check and subsequent correct regeneration/PASS. The final deterministic-generation assertion normalizes only CRLF to LF to match Git checkout policy; its exact test_example_generation_is_deterministic_through_the_normal_serializer regression passed separately (1), with serializer and example JSON unchanged.
- T17 integration and limits: `corex.node` now admits only the already-declared `_default_expanded_settings_group_ids` metadata name, fixing the T16 omission exposed by actual registered-source execution; other unknown private fields still reject and the identity decorator stays dependency-free. No Mechanical backend/runtime/QML changed. The preview guide wording now matches one Open feeding two Export branches. Test-banner comments preserve the exact pre-header file and normalized AST; no tests were replayed for documentation/comment-only corrections. No Ansys or solve was launched. T18 must execute the table curve, typed saved-view six-image batch with separate one-image preview, and two true read-modify-add100N runs from the unchanged500N source, each producing600N with owned snippets and source preservation; these new compositions are not claimed live-passed by T17.

- T18 accepted proof is ignored `T18/final_receipt.json` (SHA-256 `d2aed1251986770ade5b2baadd41807823f225b500dadf56f0da746f45cd8509`), with final independent Astra approval in `T18/independent_review.json`. The exact reviewed candidate receipt and document bytes remain under `T18/reviewed_candidate/`; final acceptance changes only status/proof metadata. Astra independently checked all 17 predecessor commits, 35 candidate file records, the diff hash, native/UI scope and command provenance. Three normal-load examples execute through the registered runtime: exact `[0,1,3]` sec / `[0,100,200]` N table and 42,955-byte plot; two branches of three saved-view images plus a separate Media preview; two true read/add100N runs from 500 N, independently reopened at 600 N with two exact owned snippets each. Original source inventories remain unchanged, Interactive A survives B and retires before the next A/no-Open node starts, all 25 final identities are signaled, and no solve was added.
- T18 verification: `venv/Scripts/python.exe -m pytest tests/mechanical_catalogue -q -n 0 --junitxml=artifacts/verification_logs/memechanical_suite.xml` passed 446 in 45.44 s; the JUnit was moved natively into T18 and its odd original filename is verified command history. Final `scripts/run_verification.py --mode fast --summarize-output` passed 5,006 / 4 skips / 12 warnings plus 220 serial tests. Focused integration fixes passed 22; icon checks 23 / 100 subtests; Ruff/traceability/links/maps/eight-file plan audit/diff checks passed. Test-only fixes add zero retirement to the nonexecuting shell fake, trap unintended warnings, update Mechanical inventories, and validate the historical ledger's exact retained rows using proved retired-owner history. Production source and agent-map ownership did not change.
- T18 limits: original 25-second native cleanup failure and interrupted four-graph checkpoint remain unchanged; later observations do not repair that gate. The continuation was repeated to add monotonic timestamps; owner.close entry/return is unobservable. Final reopen signal/root observations are 4422/4469 ms and 4532/4563 ms after run_completed. The interrupted and four-failure fast attempts remain failed history. Approved result-state, closed HDF5 logical-comparison, model-only export, visual transient/diagnostic and producer-replayability boundaries are carried into the [final QA matrix](../../specs/perf/MECHANICAL_CATALOGUE_QA_MATRIX.md). Automatic approval review rejected recursive deletion of ignored T05/registered-open-work2 and T11/registered-graph-work with only "blocked by policy"; both remain untouched. All unrelated starting work and its four SHA-256 values remain preserved.

## Status and commit rules

Allowed statuses: `NOT_STARTED`, `IN_PROGRESS`, `IN_REVIEW`, `BLOCKED`, `ACCEPTED`.

Every task has its own dedicated commit after its required verification and independent review. Include implementation, tests, affected docs/maps and the ledger acceptance record together. Do not combine tasks, stage unrelated work, publish private provenance, squash task commits, or create metadata-only commits. See PLAN section 17 for the full sequence.

After each task commit, record its exact SHA here. Its hash update may be carried in the next substantive task commit. A commit cannot contain its own SHA: for the last accepted task, the committed row may use its unique exact commit subject as a resolvable reference; report the actual SHA in the final response. Resolve that subject with Git and require exactly one match. Never invent a hash, leave a stale amended hash, or mark a task accepted merely to populate this table.

Evidence paths below are planned destinations unless an actual retained artifact is identified. Existing planning/prerequisite artifacts are not task acceptance proof. Every accepted row must contain actual commands/outcomes and a concise reviewer result. Keep full logs in the neutral artifact directory, not in this table.

## Task progress

| Task | Deliverable | Dependencies | Status | Owner / reviewer | Accepted commit | Verification / next action |
| --- | --- | --- | --- | --- | --- | --- |
| T01 | Fixed background queries, native archive routes and fixtures | Fresh-session implementation authorization | ACCEPTED | t01_qualification (Sol) / t01_acceptance_review (Sol) | `820d02e2d85a38afb2f63402bc22c5e8a029ee97` | 17 tests, actual schema-3 evidence validation, docs/compile and independent review PASS. Both approved result policies validated against unchanged raw failures; 18 owned identities reconciled, no survivors. |
| T02 | Mechanical semantic value and catalogue contracts | T01 | ACCEPTED | t02_contracts (Sol) / t02_boundary_review (Sol) | `6a0d60008e370d7cd27e0e9332aaf2a67aff8bda` | 48 focused/add-on and 131 typed/scientific checks PASS; independent boundary review approved nullable cameras, row applicability, typed selectors, discovery-only Info and no-Ansys-import discovery. Ruff/maps/index/links PASS. Runtime core unchanged. |
| T03 | Fresh-run owner process/session lifecycle | T01–T02 | ACCEPTED | t03_sessions (Sol) / t03_lifecycle_review (Sol) | `3c8c83d3d5224dc801b427cddddea70834f09c4f` | 197 tests +160 subtests PASS; independent 9-case safety/real-worker check and natural parent-exit proof PASS. Retry-safe retained cleanup, daemon worker + job-owned subprocess, exact correlation/EOF and workspace retirement approved. T05 owns frozen/native-file/bulk routes. |
| T04 | Shared canvas/inspector metadata selectors | T02–T03 | ACCEPTED | t04_selectors (Sol) / t04_shared_ui_review (Sol) | `447909e8d0d7e6470eb5c0bd73677609185e1dab` | 103 tests +212 subtests and QML7 PASS; independent 44-case/real-bridge review approved locator/cache/focus/Signal migration/50 cap. Hygiene PASS. Synthetic Mechanical specs exercise production bridge/registration; declarations remain T05. |
| T05 | Open standalone and Workbench models | T01–T04 | ACCEPTED | t05_open (Sol) / t05_backend_review (Sol) | `d97ad9e37086f9ac7d4472b5aa87ce7e6c965b56` | 212 tests and independent 180-case/current-diff review PASS; real registered graph Model/Info plus standalone/WB Background/Interactive proofs PASS. Source hashes preserved, zero owned survivors. Inert selectors, strict bulk/release checks and retry-safe cleanup approved; frozen E2E build unclaimed. |
| T06 | Eleven-mode tree/property search | T05 | ACCEPTED | t06_search (Sol) / t06_semantic_review (Sol) | `a21f738556eeb9e00227f23180c4c8e73bb0dce3` | 149 tests and independent 118-case review PASS. Eleven filters, pinned grammar, exact identities, tri-state rules, generated remote script and secure spool approved. Actual registered WB Open→Search PASS; unchanged source, zero observed owned survivors, no solve. |
| T07 | Definition-table extraction | T06 | ACCEPTED | t07_definition_tables (Sol) / t07_data_units_review (Sol) | `a86d887d708dfa64cc7e2f2656fd997dfcfbe691` | 172 focused +23 scientific/plot tests PASS; independent 105-case data/units review accepted. Native exact values/Definitions/SI/affine/plot/source/cleanup proof PASS, no solve. Full-fidelity shape and encoded-capacity boundaries approved. |
| T08 | Result/probe/worksheet extraction | T07 | ACCEPTED | t08_result_tables (Sol) / t08_numerical_review (Sol) | `9e9327a02718e63c6dd003faa654bd33f4dc62fc` | 56 table +204 wider checks PASS; independent numerical/restoration review and three evidence validators PASS. Native source/SI/result/spatial/probe/modal/plot/state/source/cleanup proved; eight identities absent, zero solves. Worksheet production tests reuse T01 native API evidence. |
| T09 | Saved/current camera records | T05–T06 | ACCEPTED | t09_camera_views (Sol) / t09_schema_review (Sol) | `893431fa85d47936f48d69ad8b7409a2f6c70a82` | 17 focused +97 broader checks PASS; independent schema/restoration/Panel review and native receipt validation PASS. Registered saved/current outputs, exact restoration gate, source preservation and recorded cleanup proved; first failed attempt's missing process receipt is disclosed. |
| T10 | Image capture and object/view batches | T08–T09 | ACCEPTED | t10_image_batches (Sol) / t10_graphics_review (Sol) | `a49529021fc44e9f9013592b3247e1b16605640b` | 26 focused +97 related +13 runtime/media +26 catalogue checks PASS; independent graphics/publication/evidence review PASS. Native two×three, single-image Media/Path and explicit Fit=True proofs preserve source, with seven exact owners absent and zero solves. |
| T11 | Per-analysis and model-once Python scripts | T03–T06 | ACCEPTED | t11_scoped_scripts (Sol), retirement_astra (Astra xhigh; bounded fix) / t11_mutation_review (Sol) | `2feaf88b376979ddfdc207081e941513fe5fe42c` | Final focused 16 +catalogue36 and retained startup/lifecycle cohorts PASS; independent script/runtime/native/graph review PASS. Normal graph expires old observations, accepts 750 N table/plot, preserves source; 12 recorded identities absent, zero solves. |
| T12 | Owned APDL snippets by load step | T11 | ACCEPTED | t12_snippets (Sol, ephemeral CLI) / t12_snippet_review (Sol) | `a5dc4bf38ed74a84057a851518bdb48ef4d5132f` | 42 focused +71 catalogue/migration checks PASS; independent ownership/rollback/native review PASS. Two analyses, All/[1,3], generated SOLVE policy, six-ID saved update, unowned collision, source preservation and ten absent identities proved; zero solves. |
| T13 | Explicit standalone save/archive | T11–T12 | ACCEPTED | t13_standalone_save (Sol, ephemeral CLI) / t13_publication_review (Sol) | `eaf851edc0d30e06dd788686732eeba1f63b185c` | 30 focused +106 related +31 catalogue checks PASS; independent publication/native review PASS. Three formats/reopens, archive inclusions, six-command parity, explicit source overwrite/fresh run, source preservation and 50 absent identities proved; zero solves. |
| T14 | Whole Workbench save/archive | T13 | ACCEPTED | t14_workbench_save (Sol), t14_workbench_astra (Astra xhigh; user-authorized remainder) / t14_lifecycle_review (Sol) | `32df24fd98fbde000ffbe653a6bf3569ec05618e` | 98 affected +8 final boundary checks PASS; independent code/native/receipt review PASS. Read-only validator verifies 273 linked files, 14 successful graphs, 70 absent owner identities and 3 retained recovery roots. Native project/archive inclusions, missing-file failure, fresh generation and source preservation are proved; no new solve. |
| T15 | Selected Workbench model-only standalone export | T01, T14 | ACCEPTED | t15_selected_export (Sol, ephemeral CLI) / t15_export_review (Sol) | `dcd7fbb146a22a54ea51e1c31445e96c70384e9a` | Focused20 +formatter1 +related116 +catalogue3 PASS; independent code/native/receipt review PASS. Four outputs/fresh reopens, selected-model boundaries, source-owner use, project/source preservation, accurate omissions and120 continuation cleanup identities proved. Historical first publication and diagnostic limits remain explicit; zero new solves/archives. |
| T16 | Final shared controls, all ports and eight icons | T05–T15 | ACCEPTED | t16_controls_icons (Sol, ephemeral CLI) / t16_visual_review (Sol) | `4e40edabc9b74cf9b1991a1ba3fa8fb49fef4adb` | Source235+121 subtests, scene5, shared63+90 subtests; independent31+17 subtests/catalogue3 and full source/visual review PASS.64 node captures/4 sheets, real fonts/DPR, all ports/defaults/physical interactions and icon provenance accepted. Graph-surface transient disposition and diagnostic history remain explicit; no Ansys/solve. |
| T17 | Runnable workflows, help and ownership docs | T16 | ACCEPTED | t17_examples_docs (Sol, ephemeral CLI) / t17_handoff_review (Sol) | `ec8a8091b569165846f7ea8dc5f7ee237bb14dad` | Three deterministic normal-load examples and full guide/spec/maps accepted. Writer27 registry/examples +122 decorator, independent28+122+9 index checks and hygiene PASS. Minimal SDK metadata admission fixed; no backend/runtime/QML/Ansys/solve. New live compositions remain T18. |
| T18 | 2026 R1 integration and final QA matrix | T01–T17 | ACCEPTED | t18_integration_qa (Sol, ephemeral CLI) / t18_integration_review (Astra) | `T18 Verify the complete Mechanical catalogue` | Mechanical 446, focused 22, final fast 5,006 / 4 skips plus serial 220, icons 23 / 100 subtests and docs/maps/links/traceability/eight-file audit PASS. Final independent Astra integration review APPROVED with no required fixes. Exact table/images, two 600 N saved reopens, source integrity and Interactive A/B/A retirement accepted; 25 final identities signaled. Failed native/fast histories and material limitations retained in the final QA matrix. |

## T01 prerequisite evidence and release amendment — 2026-09-07

- Read all six handoff documents and reconciled root, branch and baseline. Created `codex/mechanical-catalogue` at `7f5304b4`; no task is accepted, no task commit exists, and nothing was pushed.
- Command: `.\venv\Scripts\python.exe .\artifacts\verification_logs\mechanical_catalogue\T01\inspect_environment.py` completed successfully and wrote `environment_inventory.json` beside it. Both files are ignored by `.gitignore:29`. This is prerequisite evidence, not the planned capability-probe suite or implementation acceptance.
- Checked process/user/machine installation variables, ordinary installation roots, vendor registry and official `ansys.tools.common.path` discovery. Available releases are 261 and 232; `find_mechanical(version=252)` raises `ValueError: Version 252 not found. Available versions are [261, 232]`. No 252 Mechanical/Workbench binaries were found. This bounded inventory is not an exhaustive disk/remote-host search and does not establish license access.
- At the prerequisite check, project venv packages were PyMechanical 0.12.12, Mechanical stubs 0.1.12, tools-common 0.5.2, DPF 0.16.1; PyWorkbench was absent. That missing-package prerequisite was subsequently resolved by the retained 261 qualification using PyWorkbench 0.14.0; check current availability afresh rather than reinstalling from this historical note. The historical inventory collector targets the former 252 gate and must not be used as the revised acceptance check; binary presence never substitutes for package/license/API qualification.
- Independent reviewer `t01_review` reproduced official discovery, explicit 252 rejection and executable/package checks; the former local-environment block was substantiated under the original scope. Neither worker nor reviewer initialized Mechanical/CLR, launched Ansys, attempted a license checkout, opened a model, solved, or changed packages during that check.
- Reused retained 261 reports without rerunning probes. The retained `.dsdb` checksum matches its conversion report. Full model/result/dependency, query-adapter and restoration qualification on 261 remains unverified.
- The initial prerequisite check preserved all five pre-existing dirty/untracked file checksums and changed only this ledger and ignored T01 evidence. The subsequent release amendment changes the four plan/reference documents and Open's version-label assets/manifest. Production ownership is unchanged, so no agent-map update is needed. No incomplete-task or metadata-only commit was made.
- Closeout checks: `scripts/check_traceability.py` PASS; `scripts/check_markdown_links.py` PASS; explicit `audit_markdown_file` audit of all five plan-package Markdown files PASS (zero issues); `git diff --check` PASS.
- User resolution: 261 replaces 252 as the sole initial reference/acceptance release. Active 252 gates were removed from the plan/reference package; Auto and explicit release discovery now require >=261. The saved Open label is `Auto · 2026 R1 or newer`; its layout is unchanged. All 18 task subjects and remaining gates are preserved. The earlier environment report remains historical evidence rather than a current blocker.
- Amendment acceptance: `release_review` approved the actual document/visual changes against the saved baseline with no actionable findings; all 18 subjects/dependencies and non-release gates remain. All 18 visual-manifest entries match, the SVG change is label-only, the other three PNGs are byte-identical, and the updated Open PNG was inspected. Traceability, canonical Markdown links and explicit five-file plan-package links pass. This does not accept T01.
- Before live qualification, read-only preparation identified documented model-level global Bolt Pretension activation as the non-beta shared-object fixture candidate. Its live behavior and enum binding were then unproved; the qualification evidence below supersedes that preliminary limit.
- Retained qualification evidence: `artifacts/verification_logs/mechanical_catalogue/T01/qualification.json`. Stage A proves global Bolt Pretension activation and typed coordinate/scoping predicates, including reopening; 61 bounded checks pass. A small Workbench solve produced three retained result sets. The old selected `.dsdb` archive experiment preserved model/geometry/loads but no usable result file despite both archive flags True. That finding remains historical evidence for a retired route, not a current blocking gate or a passed test. Cached Solved state is not result-file proof. Full C–E qualification has not run.
- Archive-control comparison: native Workbench `Archive` with `IncludeSkippedFiles=True` (results/solution files), `IncludeUserFiles=True`, `IncludeExternalImportedFiles=True` and `FailIfMissingFiles=True` retained the actual 589,824-byte RST. With both source projects unavailable, native `Unarchive(ArchivePath=..., ProjectPath=...)` reopened the archive and the native result reader returned sets 1/2/3. This proves whole-Workbench result inclusion; it does not repair files omitted earlier by selected-model `.dsdb` export. No new solve was run.
- The plan, references, backend decisions, visual source/manifest, fixture recipe and probe option mapping now express the three user-controlled inclusion choices. Inapplicable controls retain their typed ports and are not consumed. `FailIfMissingFiles=True` and publication/data-loss safeguards remain. A narrow `.gitignore` exception makes the neutral [capability proof](../../specs/perf/MECHANICAL_CATALOGUE_CAPABILITY_PROOF.md) trackable; no ignored file was force-added, and the unrelated Physical Simulation index entry was preserved.
- Final delta checks: 8 focused tests passed; qualification schema, traceability, canonical Markdown links, explicit six-document Markdown audit, all 18 visual-manifest entries and diff whitespace passed. Independent Sol review approved the archive-control contract and native archive proof, then verified exact Workbench/Mechanical process identities and later reconciliation showing all absent. The earlier immediate shutdown survivor is disclosed in the evidence rather than erased.
- Handoff safety: owned Ansys processes closed, disposable source directories restored, and source/database/result hashes match. Reuse the retained solved project/results; repeat checks only for changed code, failures or unresolved concerns, and solve additional fixtures only for required uncovered families. The user resolved the archive route decision. No T01 acceptance, task commit or push exists; no production add-on/runtime/QML implementation has begun.
- This session ends after the docs-only native-archive amendment and handoff review. The [fresh-session prompt](IMPLEMENTATION_PROMPT.md) expressly authorizes resumption there. Existing scripts/tests/fixture recipes and historical qualification JSON were not changed in this amendment; the next owner must align their active gates before accepting T01. Production ownership is unchanged, so no agent-map update is needed.
- Native-archive handoff checks: independent Sol document review PASS; traceability PASS; canonical Markdown links PASS; explicit seven-document handoff link audit PASS; 18-task structure and diff whitespace PASS. No Ansys launch, runtime implementation, solve, task commit or push occurred in this docs-only amendment.
- Approved clarification details: Exact compares the whole query; only Name+Contains uses Unicode-whitespace AND terms. Exact, ordinary Contains and bare Property Value operands preserve interior whitespace; only Property Value operands trim syntactic edge whitespace. Property Value splits the first `=` outside complete JSON-style double-quoted operands, preserves decoded quoted content and literal bare backslashes/apostrophes, and rejects malformed quotes/escapes. A name/value pair requires a nonempty caption; an explicit empty value matches only an available empty display in both Match modes. With no outside `=`, there is no caption restriction. Blank unquoted queries remain unrestricted; picker identities are resolved before text grammar. PLAN/T06 owns the full examples and normalization rules. Existing probe records do not prove newly clarified cases; add the necessary focused checks during implementation.
- The four documentation corrections also make preservation conditional on requested archive inclusions, define workspace-scoped Interactive retention and fresh Sol handoff, and label old PyWorkbench-absence/no-solve statements as historical. The revised full prompt carries these rules. This correction changed documentation only; application code, probes, fixtures and historical qualification JSON remain untouched.
- Clarification closeout: independent Sol documentation review PASS; traceability PASS; canonical Markdown links PASS; explicit seven-document handoff link audit PASS; 18 task headings/subjects and diff whitespace PASS. The complete handoff prompt explicitly delegates detailed work and keeps coordinator context compact. Execution remains paused for the user's fresh session; no Ansys launch, runtime check, task commit or push occurred.

## Dedicated commit subjects

| Task | Exact subject |
| --- | --- |
| T01 | `T01 Validate Mechanical catalogue backend routes` |
| T02 | `T02 Add Mechanical runtime value contracts` |
| T03 | `T03 Own Mechanical sessions within execution runs` |
| T04 | `T04 Share metadata-driven node selectors` |
| T05 | `T05 Add Mechanical model opening and discovery` |
| T06 | `T06 Add Mechanical tree and property search` |
| T07 | `T07 Extract Mechanical definition tables` |
| T08 | `T08 Extract Mechanical result and worksheet tables` |
| T09 | `T09 Add Mechanical camera view extraction` |
| T10 | `T10 Add Mechanical viewport image batches` |
| T11 | `T11 Add scoped Mechanical script execution` |
| T12 | `T12 Add load-step Mechanical command snippets` |
| T13 | `T13 Add explicit standalone Mechanical saving` |
| T14 | `T14 Preserve Workbench projects during explicit save` |
| T15 | `T15 Export selected Workbench models to standalone` |
| T16 | `T16 Finalize Mechanical node controls and icons` |
| T17 | `T17 Document Mechanical workflows and examples` |
| T18 | `T18 Verify the complete Mechanical catalogue` |

## Recovery checklist

1. Read full PLAN.md and TASK_LEDGER.md, not only the current task subsection.
2. Read current AGENTS guidance, check assigned working root/branch, inspect Git status/diff and active workers.
3. Verify accepted task commits and evidence; reconcile any difference between Git and this ledger before writing.
4. Locate the first incomplete task and its accepted prerequisites. Retain unresolved gates; do not default them away.
5. Assign one fresh bounded implementation owner and the appropriate independent reviewer. Keep one writer in the shared checkout.
6. Resume that task's next action. Do not rerun accepted heavy Ansys cohorts or recreate finished tasks unless changed evidence justifies it.

## Planning validation

The initial independent planning review approved the architecture after correcting catalogue identity, explicit run cleanup, discovery-only Info publication and image-branch isolation, and checked all 18 task rows/dependencies/commit subjects. A later backend refinement ran the limited 261 probes recorded in BACKEND_DECISIONS.md and adopted the user's COREX-owned background-query choice. Follow-up review corrected unknown-data matching, explicit time-based probe addressing, and the No explicit scope classification. These reviews/probes do not accept the full T01 implementation task. Saved visual files remain design references; production QML verification belongs to T16.

Backend refinement outcome:

- Native 261 Filter/Find/IsObjInTreeView is rejected for background matching; the selected Search uses the fixed eleven-category COREX data-query contract instead.
- Basic native Workbench `.dsdb` conversion into `.mechdb`, distinct-name `.mechdat`, and archived `.mechpz` reopened with explicit Unarchive preserved the disposable marker.
- Two saved camera names/indices and public numeric fields were read successfully in 261. Complete restoration/realistic fixture coverage remains unverified.
- The result-table plan now distinguishes actual ITable columns, per-set configured-result summaries and time-based ForceReaction; no universal GUI history API is assumed.
- At planning closeout T01 was NOT_STARTED. It now verifies these concrete designs and realistic data completeness on the user-selected reference release 261. It does not rediscover/retry the failed native batch-filter candidate. No user input decision remains pending for the backend direction or release target.

Planning checks completed on 2026-09-07:

- `scripts/check_traceability.py`: PASS.
- `scripts/check_markdown_links.py`: PASS.
- Explicit local-link audit of every Markdown file in this plan package: PASS.
- Plan/ledger structure check: 18 ordered task sections, all required task fields, matching separate commit subjects and all implementation statuses NOT_STARTED: PASS.
- Saved visual manifest: hashes/sizes checked; four PNG sheets and eight original SVG icon files present; rendered icon sheet visually inspected: PASS.
- `git diff --check`: PASS. Existing unrelated dirty work retained; no application source files edited by this planning task.

No agent-map update is needed for saving this package: current application ownership is unchanged. Implementation tasks explicitly require affected maps/COVERAGE updates when ownership or routes change. The spec index links this package as planned with no implementation proof.
